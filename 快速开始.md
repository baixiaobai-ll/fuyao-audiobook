# 快速开始指南

这份指南按“扶摇”当前真实项目形态来写：主运行方式是使用 Xcode 调试 iOS App，`swift build` 只用于检查共享层。

## 5 分钟跑起来

### 第一步：准备 API 密钥

1. **通义千问 API**（文本分析）
   - 访问阿里云 DashScope / 通义千问控制台
   - 开通模型服务并创建 API Key
   - 复制密钥

2. **Azure TTS**（语音合成）
   - 访问 https://portal.azure.com/
   - 创建“语音服务”资源
   - 获取密钥和区域（如 `eastasia`）

### 第二步：配置项目

推荐使用 `Config.plist` 或 Xcode Scheme 环境变量。

1. 复制模板文件

```bash
cp Config.plist.template Config.plist
```

2. 填入你的 API Key 和服务商配置
3. 确认 `AI_PROVIDER=kimi`（默认；备选 `qwen`），以及 `TTS_PROVIDER`、`AZURE_REGION` 等值正确

### 第三步：在 Xcode 中运行 App

1. 打开 iOS 工程
2. 选择模拟器或真机
3. 直接运行 App
4. 重点测试：
   - “发现”页搜索
   - 书架导入本地 `TXT`
   - 章节播放
   - 长章节边生成边播放

### 第四步：用命令行检查共享层

如果你只想检查共享业务层是否能编译：

```bash
swift build
```

注意：
- 这个命令不负责运行完整的 iOS 界面
- 真正的 App 联调仍建议在 Xcode 中完成

## 项目状态

✅ 已完成的核心能力：

1. 文本分析模块
   - AI 智能分析
   - 对话与旁白识别
   - 角色提取
   - 情感分析
   - 场景识别
   - 分析结果缓存

2. TTS 引擎模块
   - 支持多种 TTS 服务
   - SSML 标记生成
   - 多角色音色管理
   - 智能音色分配
   - TTS 缓存系统

3. 音频混合与播放
   - 播放控制
   - 进度管理
   - 后台播放
   - 音频缓存
   - 跨段聚合进度

4. App 界面
   - 书架
   - 发现
   - 播放页
   - 我的

5. 章节播放体验
   - 在线书源章节拉取
   - 本地正文缓存
   - 长章节分批生成
   - 边生成边播放

## 当前更值得优先测试的点

1. 搜索是否直接返回线上结果
2. 长章节是否能连续播放完整内容
3. 章节切换、进度保存和再次进入播放页是否正常
4. 本地导入 TXT 后章节切分是否符合预期

## 相关文档

- `README.md`
- `Docs/API密钥配置.md`
- `Docs/开发指南.md`
- `Docs/架构设计.md`
